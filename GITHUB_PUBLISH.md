# GitHub Publish Plan

Repository name:

```text
a-share-trend-watchlist
```

Description:

```text
A-share trend watchlist generator with AkShare, Eastmoney industry mapping, and Excel visual reports.
```

Visibility:

```text
Public
```

Topics:

```text
a-share
akshare
eastmoney
stock-screener
excel-report
python
quant-research
watchlist
```

After creating an empty GitHub repository, run:

```bash
cd /Users/arosy/Documents/Codex/2026-06-30/100/outputs/a_share_watchlist_assistant
git remote add origin https://github.com/YOUR_USERNAME/a-share-trend-watchlist.git
git branch -M main
git push -u origin main
```

If the remote already exists:

```bash
git remote set-url origin https://github.com/YOUR_USERNAME/a-share-trend-watchlist.git
git push -u origin main
```

Suggested pinned files:

- `README.md`
- `outputs/trend_watchlist_2026.xlsx`
- `SERVICE_PAGE.html`
- `MONETIZATION.md`
- `SALES_COPY.md`

Suggested first release title:

```text
v0.1.0 - Excel trend watchlist prototype
```

Suggested release notes:

```text
Initial version with:

- AkShare daily data fetch
- Eastmoney industry mapping
- CSV fallback mode
- A-share watchlist screener
- Excel report output
- visual trend overview sheet
- monetization/service page draft
```
